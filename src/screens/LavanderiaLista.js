import ChecklistComodo from "./ChecklistComodo";

export default function LavanderiaLista({ voltar }) {
  return (
    <ChecklistComodo
      titulo="Lavanderia"
      voltar={voltar}
    />
  );
}
