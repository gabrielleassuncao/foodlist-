import ChecklistComodo from "./ChecklistComodo";

export default function QuartoLista({ voltar }) {
  return (
    <ChecklistComodo
      titulo="Quarto"
      voltar={voltar}
    />
  );
}
